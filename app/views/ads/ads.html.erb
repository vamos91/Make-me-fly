<div class="wrapper-profile-dashboard top-spacing">
  <%= render 'shared/subnavbar' %>
 <div style="height: 100vh;">
 <div class="container">
          <div class="row">
            <% @total_ads.each do |ad| %>
            <div class="col-xs-12 col-sm-12 col-md-4">
              <%= render ad %>
            </div>
            <% end %>
          </div>
          </div>
      </div>
</div>


