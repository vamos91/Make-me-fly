<h2 class="bg-primary text-center">Voici mes vols</h2>

<div class="container">
  <div class="row">
    <% @ads.each do |ad| %>
    <div class="col-xs-12 col-sm-4">
      <%= render ad %>
    </div>
    <% end %>
  </div>
</div>
<div class="container text-center">
<%= link_to "Retour à l'accueil", root_path, :class => "btn btn-danger" %>
</div>

