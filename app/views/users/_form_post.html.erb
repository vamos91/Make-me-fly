
<%= simple_form_for([article, post], remote: true, authenticity_token: true, :html => { :id => "new_comment_on_#{article.id}" }) do |post| %>
<%= post.input :text, label: false,placeholder: "Ecrivez un commentaire" %>
<% end %>


