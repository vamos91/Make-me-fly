<ul class="nav nav-pills nav-stacked">
      <li class="<%= active_page(:inbox) %>">
        <%= link_to conversations_path(box: 'inbox') do %>
        <span class="label label-danger pull-right"><%= @messages.count %></span>
        <em class="fa fa-inbox fa-lg"></em>
        <span>Inbox</span>
      <% end %>
      </li>
      <li class="<%= active_page(:sent) %>">
       <%= link_to conversations_path(box: 'sent') do %>
       <em class="fa fa-share-square"></em>
        <span>Sent</span>
      <% end %>
      </li>
      <li class="<%= active_page(:trash) %>">
        <%= link_to conversations_path(box: 'trash') do %>
        <em class="fa fa-trash-o fa-lg"></em>
        <span>Trash</span>
      <% end %>
      </li>
     <!--  <%= mailbox_section 'inbox', @box, @messages.count %>
      <%= mailbox_section 'sent', @box %>
      <%= mailbox_section 'trash', @box %> -->
    </ul>
