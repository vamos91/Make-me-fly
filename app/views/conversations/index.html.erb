<div class="banner-show banner-session" style="height: 200px; font-weight: lighter;">
<p>Ma messagerie</p>
</div>
<div class="marged">
  <div>
    <div>
      <% @conversations.each do |conversation| %>
        <% if conversation.sender_id == current_user.id || conversation.recipient_id == current_user.id %>
          <% if conversation.sender_id == current_user.id %>
            <% recipient = User.find(conversation.recipient_id) %>
          <% else %>
            <% recipient = User.find(conversation.sender_id) %>
          <% end %>
          <div class="conversation">
          <p>Conversation avec <%= link_to recipient.name.capitalize, conversation_messages_path(conversation)%> pour le vol du <%= conversation.ad.flight_date %> à <%= conversation.ad.address%></p>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>


<!-- <div>
  <h3>All Users</h3>
  <div>
    <% @users.each do |user| %>
      <% if user.id != current_user.id %>
      <div>
        <%= user.name %> <%= link_to 'Message me!', conversations_path(sender_id: current_user.id, recipient_id: user.id), method: 'post'%>
      </div>
      <% end %>
    <% end %>
  </div>
</div> -->
